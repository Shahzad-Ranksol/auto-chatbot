version: '3.9'

services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file: .env
    volumes:
      - uploads:/app/uploads
      - icons:/app/public/uploads/icons
      - data:/app/data

volumes:
  uploads:
  icons:
  data:
